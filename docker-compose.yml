services:
  rolescout:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      - DB_PATH=/data/rolescout.db
    volumes:
      - rolescout_data:/data
      - ./resume.md:/app/resume.md:ro
      - ./config.yaml:/app/config.yaml:ro

volumes:
  rolescout_data:
